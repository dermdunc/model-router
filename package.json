{
  "name": "model-router",
  "version": "0.1.0",
  "description": "A real CLI for the cost-aware model-routing decision: given a task's data sensitivity and complexity, recommend a model tier and the controls that come with it.",
  "type": "module",
  "bin": {
    "model-router": "./bin/model-router.mjs"
  },
  "files": [
    "bin",
    "lib",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dermdunc/model-router"
  }
}
